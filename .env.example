# Slack credentials (required)
# Get these from https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# App-Level Token for Socket Mode (starts with xapp-)
# Create in Basic Information > App-Level Tokens with connections:write scope
SLACK_APP_TOKEN=xapp-your-app-token

# AI Provider - set at least one
# Anthropic is preferred (uses Claude)
ANTHROPIC_API_KEY=sk-ant-your-key

# Or use OpenAI-compatible API (works with OpenAI, Azure, local models, etc.)
# OPENAI_API_KEY=sk-your-key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
